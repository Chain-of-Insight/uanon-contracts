(* define contract constants *)

(* define rarity constants *)
const rarityCommon1 : rarity = 5n;
const rarityCommon2 : rarity = 4n;
const rarityRare : rarity = 3n;
const rarityEpic : rarity = 2n;
const rarityLegendary : rarity = 1n;
const rarityMythic : rarity = 0n;

(* define noop for readability *)
const noOperations: list(operation) = nil;
